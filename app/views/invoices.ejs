<%- include('head'); -%>

<body>
	<div class="flex overflow-hidden bg-gray-100">

		<%- include('nav'); -%>

		<div class="main-invoices w-3/4 mx-auto px-5 mt-32 sm:mt-5 md:mt-5 lg:mt-5 min-h-screen">
			<h1 class="text-2xl font-bold leading-7 text-cool-gray-900 sm:leading-9 sm:truncate">Mes dernières factures</h1>
			<div class="invoices-block">
				<ul class="invoices-list">
				<% invoicesInfos.forEach(function(invoice) { %>
					<li class="invoice" id="invoice_<%= invoice.id %>"><a href="<%= invoice.url %>" target="_blank"><%= invoice.date %></a></li>
				<% }) %>
				</ul>
			</div>
		</div>
	</div>
</body>